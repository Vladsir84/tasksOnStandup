export const setButton = (buttonText) => {
  let bodyElem = document.querySelector('body');
  bodyElem.innerHTML = '<button>button text</button>'
//   bodyElem.textContent = '<button>button text</button>'
  buttonText = text;
}